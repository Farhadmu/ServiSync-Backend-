$kind: http-request
description: ""
url: "{{baseUrl}}/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "tech1@servisync.com",
      "password": "Tech@123"
    }
scripts:
  - type: afterResponse
    code: "const res = pm.response.json();\r

      if (res.data?.accessToken) {\r

      \  pm.collectionVariables.set(\"technicianToken\", res.data.accessToken);\r

      }"
    language: text/javascript
order: 3098
